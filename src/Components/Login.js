import React from "react";

const Login = () => {

    const handleLogin = () => {
        
    }

    return (
        <div className="">
            Metamask Login
            <button onClick={handleLogin}>Login</button>
        </div>
    )
}

export default Login;